

spring.datasource.password=irrelevant

# Replaces spring.jpa.hibernate.ddl-auto=create-drop: Spring Data JDBC generates no DDL,
# so schema.sql creates the tables. "always" is required because the Testcontainers
# datasource is not an embedded one.
spring.sql.init.mode=always

jwt.secret=bXktc3VwZXItc2VjcmV0LWtleS1mb3ItdGVzdHMtMTIzNDU2
