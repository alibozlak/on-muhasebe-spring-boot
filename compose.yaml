services:
  postgres:
    image: 'postgres:17-alpine'
    environment:
      POSTGRES_DB: test_db
      POSTGRES_USER: bozlak
      POSTGRES_PASSWORD: secret
    ports:
      - '5433:5432'
    volumes:
      - db_pre-accounting:/var/lib/postgresql/data

volumes:
  db_pre-accounting:

